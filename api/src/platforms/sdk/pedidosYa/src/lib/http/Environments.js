const Environments = Object.freeze({
  /**
   * Default environment for development only
   */
  DEVELOPMENT: 'DEVELOPMENT',

  /**
   * Pre-production environment
   */
  STAGING: 'STAGING',

  /**
   * Production environment
   */
  PRODUCTION: 'PRODUCTION'
});
module.exports = Environments;
