const RestaurantState = Object.freeze({
  /**
   * State of the restaurant when is online
   */
  ONLINE: 'ONLINE',

  /**
   * State of the restaurant when is offline
   */
  OFFLINE: 'OFFLINE'
});
module.exports = RestaurantState;
